package queues;
/*
public class Queue {
        // these members are now private

        private String q[];                                       // this array holds the queue
        private int putloc;
        private int getloc;                             // the put and get indices

        Queue(Queue size) {                                       // this is a contructor which is used to create an object
            q = new Queue[size];                                 // allocate memory for queue
            putloc = getloc = 0;}                               // index

    public Queue(RunInter r1) {
    }

    // Put a characer into the queue.
        void put(String ch) {                                 //method to see whether the you can put anything else in
            if(putloc==q.length) {
                System.out.println(" -- Queue is full.");
                return;}
            q[putloc++] = ch;
        }


        // Get a character from the queue.
        String get() {                                            //method to change
            if(getloc == putloc) {
                System.out.println(" -- Queue is empty.");
                return ("");
            }

            return q[getloc++];
        }
    }
*/
